#! node-api-node_modules-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: node-api-node-modules
    namespace: confetti-server
    labels:
        com.docker.compose.service: node-api
        com.docker.compose.volume: node_modules
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
